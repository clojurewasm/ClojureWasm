(letfn [(solve2 [start max-depth max-states] (loop [front (list [start []]) back () visited #{start}] (cond (and (empty? front) (empty? back)) [:exhausted (count visited)] (>= (count visited) max-states) [:capped (count visited)] (empty? front) (recur (reverse back) () visited) :else (let [head (first front)] (if-not (vector? head) [:corrupt head] (let [[state path] head front (rest front)] (cond (>= state 30) [:won path] (>= (count path) max-depth) (recur front back visited) :else (let [next-states (for [d [1 2 3] :let [s2 (+ state d)] :when (not (visited s2))] [s2 (conj path d)])] (recur front (into back next-states) (into visited (map first next-states)))))))))))] (solve2 0 24 1000))
;;=> [:won [3 3 3 3 3 3 3 3 3 3]]
