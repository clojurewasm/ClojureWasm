`(1 2 ~(+ 1 2))
;;=> (1 2 3)
`[1 ~(* 2 2) 3]
;;=> [1 4 3]
`(~@(list 1 2 3))
;;=> (1 2 3)
`(0 ~@(list 1 2 3) 4)
;;=> (0 1 2 3 4)
(let [x 5] `(~x ~(* x 2)))
;;=> (5 10)
(let [xs [1 2 3]] `[~@xs ~@xs])
;;=> [1 2 3 1 2 3]
`(:k1 :k2 ~(keyword "k3"))
;;=> (:k1 :k2 :k3)
(let [m {:a 1}] `[~@(keys m) ~@(vals m)])
;;=> [:a 1]
`(1 ~@[] 2)
;;=> (1 2)
`(~(inc 0) ~(inc 1) ~(inc 2))
;;=> (1 2 3)
`(a b c)
;;=> (user/a user/b user/c)
`(if ~(odd? 3) :y :z)
;;=> (if true :y :z)
`(+ 1 2)
;;=> (clojure.core/+ 1 2)
`(map inc coll)
;;=> (clojure.core/map clojure.core/inc user/coll)
`(let [x 1] x)
;;=> (clojure.core/let [user/x 1] user/x)
`(.method obj)
;;=> (.method user/obj)
(= `foo (quote user/foo))
;;=> true
(= `+ (quote clojure.core/+))
;;=> true
(= `if (quote if))
;;=> true
`(when ~(pos? 1) :ok)
;;=> (clojure.core/when true :ok)
`(defn f [x] (* x 2))
;;=> (clojure.core/defn user/f [user/x] (clojure.core/* user/x 2))
`(fn* [a] (recur a))
;;=> (fn* [user/a] (recur user/a))
;; D-228: nested `` ` `` preserves the inner template as data (an inner ~ is
;; consumed at the inner level; ~~ escapes one level back to the outer eval).
`(a `(b ~(+ 1 2)))
;;=> (user/a (clojure.core/seq (clojure.core/concat (clojure.core/list (quote user/b)) (clojure.core/list (clojure.core/+ 1 2)))))
`(a `(b ~~(+ 1 2)))
;;=> (user/a (clojure.core/seq (clojure.core/concat (clojure.core/list (quote user/b)) (clojure.core/list 3))))
`(a `(b ~@(list 1 2)))
;;=> (user/a (clojure.core/seq (clojure.core/concat (clojure.core/list (quote user/b)) (clojure.core/list 1 2))))
