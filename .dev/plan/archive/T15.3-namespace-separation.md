# T15.3: Namespace Separation (walk, template)

## Goal

Separate clojure.walk and clojure.template into their proper namespaces,
matching upstream Clojure directory structure.

## Plan

### Phase 0: Move core.clj to upstream structure

1. Move src/clj/core.clj → src/clj/clojure/core.clj
2. Update bootstrap.zig embedFile path
3. Verify tests pass

### Phase A: clojure.walk separation

1. Create src/clj/clojure/walk.clj
2. Add loadWalk() to bootstrap.zig
3. Add bootstrap call to main.zig
4. Remove walk functions from core.clj
5. Update test.clj if needed

### Phase B: clojure.template separation

1. Create src/clj/clojure/template.clj
2. Add loadTemplate() to bootstrap.zig
3. Add bootstrap call to main.zig
4. Remove template functions from core.clj

### Phase C: Documentation

- Update vars.yaml
- Add D## entry
- Update memo.md

## Log

- Phase 0: Moved core.clj to src/clj/clojure/core.clj, updated embedFile path
- Phase A: Created walk.clj, added loadWalk() to bootstrap, removed from core.clj
- Phase B: Created template.clj, added loadTemplate() to bootstrap, removed from core.clj
- Phase C: Added D54 to decisions.md, updated impl-tiers.md, memo.md, CLAUDE.md
- All tests pass
